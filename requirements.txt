# Android TV Box Home Assistant Integration Requirements
# Install these dependencies in your Ubuntu container environment
# Note: Home Assistant is already installed, so it's not included here

# Core dependencies
uiautomator2>=2.16.23

# Web server dependencies
aiohttp>=3.8.0
aiohttp-cors>=0.7.0

# MQTT dependencies
paho-mqtt>=1.6.0

# Other dependencies
voluptuous>=0.13.0
